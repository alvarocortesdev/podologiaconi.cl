{
  "name": "podologiaconi-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run generate --workspace=server && npm run build --workspace=client",
    "postinstall": "npm run generate --workspace=server"
  },
  "engines": {
    "node": "18.x"
  }
}