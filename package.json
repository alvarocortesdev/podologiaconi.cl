{
  "name": "podologiaconi-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "cd server && npm install && npx prisma generate && cd ../client && npm install && npm run build",
    "postinstall": "cd server && npm install && npx prisma generate"
  },
  "engines": {
    "node": "18.x"
  }
}