{
  "name": "podologiaconi-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "cd client && npm run build",
    "postinstall": "cd server && npx prisma generate"
  }
}